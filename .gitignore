# Rust / Cargo
/target
**/*.rs.bk

# Agent guidance and tooling (local only)
CLAUDE.md
AGENTS.md
AGENTS.override.md
.agents/
.claude/
.codex/
.idea/
.vscode/
*.iml
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Environment / local config
.env
.env.local
.env.*.local

# Merge / backup artifacts
*.orig
*.rej

# Personal devlog
docs/devlog/
docs/context/
docs/archive/

# Personal audit / dev scripts (not shipped; rerun locally)
scripts/

# Demo capture artifacts (loam-app capture module + legacy --capture-{apng,gif} flags)
/captures/
*.apng
*.gif
/out.*

/dist/
crates/*/dist/

# Stray build/diagnostic logs
*.log
