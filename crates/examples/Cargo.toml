[package]
name = "examples"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Loam example bins: hero, tesseract, rubiks4d, twospace."
publish = false

[dependencies]
ab_glyph = "0.2"
glam.workspace = true
tracing.workspace = true

loam-app     = { path = "../loam-app",     version = "0.1.0" }
loam-math    = { path = "../loam-math",    version = "0.1.0" }
loam-physics = { path = "../loam-physics", version = "0.1.0", default-features = false, features = ["r4"] }
loam-render  = { path = "../loam-render",  version = "0.1.0" }
loam-runtime = { path = "../loam-runtime", version = "0.1.0", features = ["physics"] }
loam-shape   = { path = "../loam-shape",   version = "0.1.0" }
loam-text    = { path = "../loam-text",    version = "0.1.0" }
loam-time    = { path = "../loam-time",    version = "0.1.0" }

[lints]
workspace = true
